# .env.example
# Documented environment variables for ROC Agents Workspace

# --- REQUIRED ---
# The server refuses to start without this. Minimum 12 characters.
# Generate:  openssl rand -base64 24
WEB_PASSWORD=

# Network binding. Default is 127.0.0.1 (loopback only) and that is the safe choice.
# Only change it if the network is trusted, e.g. a Tailscale address:
#   HOST=100.x.y.z
HOST=127.0.0.1

# Shell command guard for exec / terminal_manager.
#   enforce (default) = block dangerous commands
#   warn              = log only, still executes  (NOT recommended)
#   off               = disable entirely          (NOT recommended)
# This is a seatbelt against agent mistakes, not a sandbox. See server/commandGuard.ts.
SHELL_GUARD=enforce

# Execution of self_develop_capability snippets via new Function().
# Default false: snippets would run with full Node privileges, bypassing the
# shell guard entirely. Enable only if you understand the risk, then restart.
SELF_DEV_EXECUTE=false

GEMINI_API_KEY=
APP_URL=http://localhost:3000
PORT=3000
NODE_ENV=development

# AI Provider Keys
CF_SHERLOCK_KEY=
GITLAB_DUO_KEY=
REQUESTY_API_KEY=
GROQ_KEY=
OPENAI_API_KEY=
OR_KEY=
DEEPSEK_API_KEY=
X_GOOG_API_KEY=

# Cloud & Database Services
AIVEN_PG_URI=
AIVEN_PG2_URI=
NEON_URI=
MONGO_URI=

# Auth Services
CLERK_PK=
CLERK_SK=
CLERK_SECRET_KEY=

# Infrastructure & Integration Services
CF_ACCOUNT_ID=
CF_ZONE_ID=
SOLACE_HOST=
TAILSCALE_KEY=
